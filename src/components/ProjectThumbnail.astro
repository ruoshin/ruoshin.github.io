---
import { Image } from "astro:assets";
import ExternalLink from "@/components/ExternalLink.astro";
import type { ProjectImage } from "@/data/projects";
import type { Locale } from "@/lib/i18n";

interface Props {
  img: ProjectImage;
  locale: Locale;
}

const { img, locale } = Astro.props;
---

<li
  class="rounded-2xl overflow-hidden border border-[var(--color-border)] bg-[var(--color-subtle)] hover:-translate-y-1 hover:shadow-[0_16px_32px_-20px_var(--color-secondary)] transition-all duration-300"
>
  {
    img.href ? (
      <ExternalLink href={img.href} class="block hover:opacity-90 transition-opacity">
        <Image
          src={img.src}
          alt={img.alt[locale]}
          widths={[400, 800, 1200]}
          sizes="(max-width: 768px) 50vw, 33vw"
          loading="lazy"
          class="w-full h-auto block"
        />
      </ExternalLink>
    ) : (
      <Image
        src={img.src}
        alt={img.alt[locale]}
        widths={[400, 800, 1200]}
        sizes="(max-width: 768px) 50vw, 33vw"
        loading="lazy"
        class="w-full h-auto block"
      />
    )
  }
</li>
